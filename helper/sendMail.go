package helper

// import (
// 	"fmt"
// 	"net/smtp"
// )

func SendMailSMTP(toMailAddress string, message []byte) error {
	// senderEmailAddress := ""
	// smtpUserName := ""
	// smtpPassword := ""
	// smtpHost := ""
	// smtpPort := 124
	// auth := smtp.PlainAuth("", smtpUserName, smtpPassword, smtpHost)
	// to := []string{toMailAddress}
	// serverAddr := fmt.Sprintf("%s:%d", smtpHost, smtpPort)

	return nil //smtp.SendMail(serverAddr, auth, senderEmailAddress, to, message)
}
